import { Component } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';

@Component({
    moduleId: module.id,
  selector: 'page1-welcome',
  templateUrl: `page1-welcome.component.html`,
})
export class Page1WelcomeComponent  { }
